import React from "react"
import { Row, Col, Container } from "react-bootstrap"
import styled from "styled-components"

const Stack = () => {
    return (
        <Container>
            <Row>
                <h1>This is the stack section.</h1>
            </Row>
        </Container>
    )
}

export default Stack
